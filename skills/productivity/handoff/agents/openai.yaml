interface:
  display_name: "Handoff"
  short_description: "Prepare context for a fresh task session"
  default_prompt: "Use $handoff to prepare context for the next session focused on this topic."

policy:
  allow_implicit_invocation: false
