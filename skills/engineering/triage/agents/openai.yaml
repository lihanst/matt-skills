interface:
  display_name: "Triage"
  short_description: "Turn incoming issues into agent-ready briefs"
  default_prompt: "Use $triage to categorize and prepare these incoming issues for implementation."

policy:
  allow_implicit_invocation: false
