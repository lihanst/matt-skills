interface:
  display_name: "To Tickets"
  short_description: "Split a plan into dependency-aware tickets"
  default_prompt: "Use $to-tickets to split this plan or spec into dependency-aware tickets."

policy:
  allow_implicit_invocation: false
