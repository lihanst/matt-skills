interface:
  display_name: "Implement Loop"
  short_description: "Implement and iterate through review findings"
  default_prompt: "Use $implement-loop and $code-review-loop to implement this work, complete review, and create a pull request."

policy:
  allow_implicit_invocation: false
