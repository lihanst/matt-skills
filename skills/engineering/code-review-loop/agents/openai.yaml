interface:
  display_name: "Code Review Loop"
  short_description: "Review and fix until every finding is resolved"
  default_prompt: "Use $code-review-loop to review and fix these changes until every confirmed finding is resolved."

policy:
  allow_implicit_invocation: false
