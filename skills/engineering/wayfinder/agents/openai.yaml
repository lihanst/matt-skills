interface:
  display_name: "Wayfinder"
  short_description: "Map and resolve multi-session investigations"
  default_prompt: "Use $wayfinder to map this large effort into a sequence of investigation tickets."

policy:
  allow_implicit_invocation: false
