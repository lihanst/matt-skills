interface:
  display_name: "Grill with Docs"
  short_description: "Sharpen a plan and capture decisions"
  default_prompt: "Use $grill-with-docs to sharpen this plan and capture the resulting decisions."

policy:
  allow_implicit_invocation: false
